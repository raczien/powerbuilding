import 'package:flutter/material.dart';

class GroupSplitter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 20,
      child: Divider(
        color: Colors.white,
      ),
    );
  }
}
